import styled from 'styled-components';

export const UnderlineHr = styled.div`
  width: 100%;
  height: ${(props) => props.theme.paddingSize.sm};
  background-color: ${(props) => props.theme.colors.black};
`;
